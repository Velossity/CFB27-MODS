node_modules/
dist/
build/
*.log
.env
.env.*
.DS_Store
Thumbs.db

# Local game copies — never commit real saves or install folders
**/saves/
**/game-files/
*.sav
*.bin
*.mc02

# Tool output
_Shared/tools/**/output/
_Shared/tools/**/reports/

# Local shortcut (recreate if missing — points at Desktop MMC install)
# Keep tracked for this machine; ignore if you prefer:
# *.lnk

# Frosty / MMC local caches (never commit)
**/Caches/
*.fbproject.bak
